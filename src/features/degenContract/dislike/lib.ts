import type { PayableOverrides } from '@ethersproject/contracts'

export class createPost {
  createPost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  dislikePost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  getPost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  likePost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  owner (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  postCost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  posts (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  setPostCost (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  subscribeToAuthor (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  unsubscribeFromAuthor (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }

  withdrawBalance (value: string, overrides: PayableOverrides) {
    console.log(value, overrides)
  }
}
